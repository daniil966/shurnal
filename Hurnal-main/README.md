Журнал Кузьмин Даниил ИС-323 🌆
Первые строки кода определяют принадлежность файла к основному пакету программы:
go
1
package main

Это стандартное объявление точки входа в любую Go-программу.

Импорт необходимых пакетов:

go
1234567
import (    "bufio"    "fmt"    "os"    "strconv"    "strings")

Каждый из подключённых пакетов выполняет свою важную роль:

bufio — обеспечивает буферизированный ввод, что делает чтение данных из консоли более гибким и устойчивым к ошибкам.
fmt — отвечает за форматированный вывод и ввод (в связке с bufio).
os — предоставляет доступ к стандартному вводу (os.Stdin) и другим системным функциям.
strconv — используется для безопасного преобразования строк в числа и наоборот.
strings — позволяет обрабатывать текстовые данные: удалять пробелы, разбивать строки и т.д.
Определение структуры Student:

go
12345
type Student struct {    FIO    string    Grades []int    Avg    float64}

Эта структура служит для хранения данных о студенте:

FIO — полное имя студента (Фамилия и Имя), представлено строкой.
Grades — срез целых чисел, содержащий все оценки студента (от 1 до 5).
Avg — средний балл, вычисляемый автоматически и хранящийся как число с плавающей точкой.
Метод calculateAverage для вычисления среднего балла:

go
1
func (s *Student) calculateAverage()

Объявляется как метод, привязанный к указателю на структуру Student, чтобы изменения (s.Avg) сохранялись в исходном объекте.

go
1234
if len(s.Grades) == 0 {    s.Avg = 0.0    return}

Если у студента нет оценок, средний балл устанавливается в 0.0, и метод завершается — это предотвращает деление на ноль.

go
1234
sum := 0for _, grade := range s.Grades {    sum += grade}

Вычисляется сумма всех оценок с помощью цикла по срезу Grades.

go
1
s.Avg = float64(sum) / float64(len(s.Grades))

Средний балл рассчитывается как отношение суммы оценок к их количеству. Оба операнда явно приводятся к float64 для корректного деления.

Функция addStudent — добавление нового студента:

go
1
func addStudent(students map[string]Student, reader *bufio.Reader)

Принимает карту студентов и буферизированный ридер для ввода.

go
123
fmt.Print("Введите фамилию и имя студента: ")fio, _ := reader.ReadString('\n')fio = strings.TrimSpace(fio)

Пользователю предлагается ввести ФИО, после чего ввод читается и очищается от переносов строк и пробелов.

go
1234
if _, exists := students[fio]; exists {    fmt.Println("Студент с таким ФИО уже существует.")    return}

Проверяется уникальность ФИО: если студент уже есть в журнале — операция прерывается.

go
12
var grades []intfmt.Println("Введите оценки студента через пробел. Нажмите Enter еще раз для завершения.")

Инициализируется пустой срез оценок и выводится подсказка.

go
1234
for {    fmt.Print("> ")    input, _ := reader.ReadString('\n')    input = strings.TrimSpace(input)

Запускается цикл ввода оценок с приглашением >. Каждая строка очищается.

go
1234567
    if input == "" {        if len(grades) == 0 {            fmt.Println("Оценки не были введены. Введите оценки или нажмите Enter еще раз для пропуска.")            continue        }        break    }

Пустой ввод завершает цикл, но только если уже есть хотя бы одна оценка. Это предотвращает создание студента без оценок по ошибке.

go
1
gradesStrSlice := strings.Fields(input)

Строка разбивается на отдельные «слова» (оценки), используя пробелы как разделители.

go
123456789101112
for _, gradeStr := range gradesStrSlice {    grade, err := strconv.Atoi(gradeStr)    if err != nil {        fmt.Printf("Некорректный ввод оценки '%s'. Пожалуйста, вводите только числа.\n", gradeStr)        continue    }    if grade < 1 || grade > 5 {        fmt.Printf("Оценка %d вне допустимого диапазона (1-5)\n", grade)        continue    }    grades = append(grades, grade)}

Каждая строка преобразуется в число:

При ошибке — выводится предупреждение, оценка пропускается.
Если оценка не в диапазоне 1–5 — тоже пропускается с уведомлением.
Корректные оценки добавляются в срез.
go
1234567
student := Student{    FIO:    fio,    Grades: grades,}student.calculateAverage()students[fio] = studentfmt.Println("Студент", fio, "успешно добавлен!")

Создаётся объект Student, вычисляется его средний балл, и он сохраняется в карте по ключу ФИО.

Функция фильтрации студентов по среднему баллу:

go
1
func filterStudentsByAvg(students map[string]Student, threshold float64) []Student

Возвращает срез студентов, чей средний балл ниже заданного порога.

go
1234567
filteredStudents := []Student{}for _, student := range students {    if student.Avg < threshold {        filteredStudents = append(filteredStudents, student)    }}return filteredStudents

Происходит проход по всем студентам и отбор подходящих по условию.

Вспомогательные функции вывода:

go
1
func printStudentInfo(student Student)

Форматированный вывод данных одного студента: ФИО, оценки, средний балл с двумя знаками после запятой.

go
1
func printAllStudents(students map[string]Student)

Выводит заголовок и поочерёдно информацию о всех студентах, используя printStudentInfo.

Главная функция main:

go
12
students := make(map[string]Student)reader := bufio.NewReader(os.Stdin)

Инициализируется карта для хранения студентов и буферизированный читатель.

go
1
fmt.Println("Добро пожаловать в журнал!")

Приветственное сообщение при запуске программы.

go
1234
for {    fmt.Print("\nВведите команду (help - для вывода всех команд): ")    command, _ := reader.ReadString('\n')    command = strings.TrimSpace(command)

Бесконечный цикл меню: программа постоянно ожидает ввода команды.

Обработка команд через switch:

"add" — вызывает addStudent для добавления нового студента с валидацией ФИО и оценок.
"list" — выводит всех студентов или сообщение об их отсутствии.
"filter" — запрашивает пороговое значение среднего балла, проверяет корректность ввода (strconv.ParseFloat), фильтрует студентов и выводит результат.
"help" — отображает справку по всем доступным командам:
12345
  add    - Добавить нового студента.  list   - Вывести информацию о всех студентах.  filter - Отфильтровать студентов по среднему баллу (ниже заданного порога).  help   - Показать список доступных команд.  exit   - Выйти из программы.

"exit" — завершает программу с прощальным сообщением.
Любая другая команда — выводит сообщение об ошибке и предлагает воспользоваться help.
✅ Итоги и особенности реализации:

Проект полностью соответствует требованиям учебной практики:

Реализована интерактивная консольная система управления журналом студентов.
Поддерживается добавление студентов с валидацией (уникальность ФИО, корректность оценок).
Реализована фильтрация по среднему баллу с защитой от некорректного ввода.
Используется модульная структура кода: каждый функционал выделен в отдельную функцию.
Программа устойчива к ошибкам: обрабатываются пустые вводы, неверные числа, выход за диапазон оценок.
Интерфейс интуитивно понятен, содержит справку и подсказки.