Журнал студентов Кузьмин Даниил ИС-323 🌆
📌 Описание
Проект представляет собой консольное приложение на языке Go, предназначенное для ведения журнала успеваемости студентов.
Программа написана с использованием модульной структуры, где каждый функционал вынесен в отдельную функцию.
Цель: реализовать добавление студентов с оценками, автоматический расчёт среднего балла и фильтрацию по заданному порогу с пользовательским интерфейсом в консоли.

🧩 Структура проекта
student-journal/
├── main.go # Основной файл с меню и логикой ввода/вывода
├── student.go # Структура Student и метод calculateAverage
├── handlers.go # Функции обработки команд (add, list, filter)
├── go.mod # Модуль Go
└── README.md # Документация

🧾 Описание кода (анализ по строкам)

🔹 main.go

go
12
1
package main
Первые строки кода определяют принадлежность файла к главному пакету main. Это точка входа в программу.

go
12345678
1234567
import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)
Импорт пакетов:

bufio — для буферизированного ввода, обеспечивающего устойчивость к ошибкам при чтении.
fmt — для форматированного ввода и вывода в консоль.
os — для доступа к стандартному вводу (os.Stdin).
strconv — для безопасного преобразования строк в числа.
strings — для обработки текста: удаление пробелов, разбиение на части.
go
123
12
func main() {
	fmt.Println("Добро пожаловать в журнал!")
Через функцию main происходит вход в программу.
Сначала выводится приветственное сообщение.

go
1234567891011121314151617181920212223242526
1234567891011121314151617181920212223242526272829
	students := make(map[string]Student)
	reader := bufio.NewReader(os.Stdin)

	for {
		fmt.Print("\nВведите команду (help - для вывода всех команд): ")
		command, _ := reader.ReadString('\n')
		command = strings.TrimSpace(command)

		switch command {

Основной цикл программы:

Создаётся карта students для хранения данных о студентах.
Инициализируется буферизированный читатель.
В бесконечном цикле запрашивается команда пользователя.
Конструкция switch обрабатывает введённую команду:
case "add" — вызывает addStudent() для добавления нового студента.
case "list" — вызывает printAllStudents() для вывода всех записей.
case "filter" — вызывает handleFilter() для фильтрации по среднему баллу.
case "help" — вызывает printHelp() для отображения справки.
case "exit" — завершает программу.
default — выводит сообщение о неизвестной команде.
🔹 student.go

go
123456
12345
type Student struct {
	FIO    string
	Grades []int
	Avg    float64
}
Определение структуры Student для хранения данных о студенте:

FIO — полное имя студента (строка).
Grades — срез оценок (целые числа от 1 до 5).
Avg — средний балл (число с плавающей точкой).
go
12
1
func (s *Student) calculateAverage()
Объявление метода, привязанного к указателю на структуру Student, чтобы изменения поля Avg сохранялись в исходном объекте.

go
12345
1234
if len(s.Grades) == 0 {
	s.Avg = 0.0
	return
}
Если у студента нет оценок, средний балл устанавливается в 0.0, и метод завершается — это предотвращает деление на ноль.

go
12345
1234
sum := 0
for _, grade := range s.Grades {
	sum += grade
}
Вычисляется сумма всех оценок с помощью цикла по срезу Grades.

go
12
1
s.Avg = float64(sum) / float64(len(s.Grades))
Средний балл рассчитывается как отношение суммы оценок к их количеству. Оба операнда явно приводятся к float64 для корректного деления.

🔹 handlers.go

go
12
1
func addStudent(students map[string]Student, reader *bufio.Reader)
Функция для добавления нового студента:

go
1234
123
fmt.Print("Введите фамилию и имя студента: ")
fio, _ := reader.ReadString('\n')
fio = strings.TrimSpace(fio)
Пользователю предлагается ввести ФИО, после чего ввод читается и очищается от переносов строк и пробелов.

go
12345
1234
if _, exists := students[fio]; exists {
	fmt.Println("Студент с таким ФИО уже существует.")
	return
}
Проверяется уникальность ФИО: если студент уже есть в журнале — операция прерывается.

go
123
12
var grades []int
fmt.Println("Введите оценки студента через пробел. Нажмите Enter еще раз для завершения.")
Инициализируется пустой срез оценок и выводится подсказка.

go
12345678910111213141516171819202122232425262728293031323334353637
123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354
for {
	fmt.Print("> ")
	input, _ := reader.ReadString('\n')
	input = strings.TrimSpace(input)

	if input == "" {
		if len(grades) == 0 {
			fmt.Println("Оценки не были введены. Введите оценки или нажмите Enter еще раз для пропуска.")
			continue

Запускается цикл ввода оценок с приглашением >. Каждая строка очищается, разбивается на отдельные значения и проверяется:

При ошибке преобразования — выводится предупреждение, оценка пропускается.
Если оценка не в диапазоне 1–5 — тоже пропускается с уведомлением.
Корректные оценки добавляются в срез.
После завершения ввода создаётся объект Student, вычисляется его средний балл, и он сохраняется в карте по ключу ФИО.

go
12
1
func filterStudentsByAvg(students map[string]Student, threshold float64) []Student
Функция фильтрации студентов по среднему баллу:

go
12345678
1234567
filteredStudents := []Student{}
for _, student := range students {
	if student.Avg < threshold {
		filteredStudents = append(filteredStudents, student)
	}
}
return filteredStudents
Происходит проход по всем студентам и отбор подходящих по условию (Avg < threshold).

go
12
1
func printStudentInfo(student Student)
Форматированный вывод данных одного студента: ФИО, оценки, средний балл с двумя знаками после запятой.

go
12
1
func printAllStudents(students map[string]Student)
Выводит заголовок и поочерёдно информацию о всех студентах, используя printStudentInfo.

go
12
1
func printHelp()
Вывод справки по доступным командам:

12345
add    - Добавить нового студента.
list   - Вывести информацию о всех студентах.
filter - Отфильтровать студентов по среднему баллу (ниже заданного порога).
help   - Показать список доступных команд.
exit   - Выйти из программы.
go
12
1
func handleFilter(students map[string]Student, reader *bufio.Reader)
Функция обрабатывает команду filter:

Запрашивает пороговое значение среднего балла.
Проверяет корректность ввода с помощью strconv.ParseFloat.
Вызывает filterStudentsByAvg и выводит результат или сообщение об отсутствии подходящих студентов.
📝 Вывод
Проект успешно реализует все требования ПР №3/5:
✅ Добавление студентов с валидацией ФИО и оценок
✅ Автоматический расчёт среднего балла
✅ Фильтрация по среднему баллу
Код разделён на логические функции.
Поддерживается ввод оценок от 1 до 5.
Удобный интерфейс в консоли с подсказками и справкой.
Код соответствует требованиям ПР №5 — функционал разделён на функции.
