# Журнал успеваемости студентов 📚

**Автор:** Кузьмин Даниил ИС-323

## 🎯 Описание
Проект представляет собой консольное приложение на языке Go, предназначенное для ведения журнала успеваемости студентов. Программа написана с использованием модульной структуры, где каждый функционал вынесен в отдельную функцию.

**Цель:** реализовать добавление студентов с оценками, автоматический расчёт среднего балла и фильтрацию по заданному порогу с пользовательским интерфейсом в консоли.

## 🚀 Быстрый старт

### Предварительные требования
- Установленный Go (версия 1.19 или выше)
- Git (для клонирования репозитория)

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/student-journal.git
cd student-journal

# 2. Собрать проект
go build -o student-journal main.go student.go handlers.go

# 3. Запустить приложение
./student-journal

# Или запустить напрямую
go run main.go student.go handlers.go
Структура проекта
text
student-journal/
├── main.go          # Основной файл с меню и логикой ввода/вывода
├── student.go       # Структура Student и метод calculateAverage
├── handlers.go      # Функции обработки команд (add, list, filter)
├── go.mod           # Модуль Go
└── README.md        # Документация
📖 Команды Git для проекта
Инициализация репозитория
bash
# Создание нового репозитория
git init
echo "# Журнал успеваемости студентов" > README.md

# Создание структуры файлов
touch main.go student.go handlers.go go.mod

# Добавление файлов в отслеживание
git add .
git commit -m "Initial commit: базовая структура проекта"

# Связывание с удаленным репозиторием
git remote add origin https://github.com/yourusername/student-journal.git
git branch -M main
git push -u origin main
Рабочий процесс разработки
bash
# Создание ветки для новой функциональности
git checkout -b feature/add-export-functionality

# Добавление изменений
git add handlers.go
git commit -m "feat: добавить экспорт в CSV формат"

# Возврат в основную ветку и слияние
git checkout main
git merge feature/add-export-functionality --no-ff -m "Merge feature: экспорт в CSV"
git branch -d feature/add-export-functionality

# Отправка изменений
git push origin main
Управление версиями
bash
# Создание тега версии
git tag -a v1.1.0 -m "Версия 1.1.0: добавлена фильтрация по оценкам"
git push origin --tags

# Создание архива для сдачи проекта
git archive --format zip --output student-journal-v1.0.0.zip main
git archive --format zip --output student-journal-source-v1.0.0.zip --prefix=student-journal/ HEAD
🖥️ Использование программы
Доступные команды
text
Добро пожаловать в журнал!

Введите команду (help - для вывода всех команд): help

Доступные команды:
add    - добавить нового студента
list   - вывести информацию обо всех студентах
filter - отфильтровать студентов по среднему баллу
help   - показать список доступных команд
exit   - выйти из программы
Пример работы
text
Введите команду (help - для вывода всех команд): add
Введите фамилию и имя студента: Иванов Петр
Введите оценки студента через пробел. Нажмите Enter еще раз для завершения.
> 5 4 5
> 4 3
> 
Студент Иванов Петр успешно добавлен с 5 оценками.

Введите команду (help - для вывода всех команд): list
=== Список всех студентов ===
Студент: Иванов Петр
Оценки: [5 4 5 4 3]
Средний балл: 4.20
----------------------------------------

Введите команду (help - для вывода всех команд): filter
Введите пороговое значение среднего балла: 4.5
=== Студенты со средним баллом ниже 4.5 ===
Студент: Иванов Петр
Оценки: [5 4 5 4 3]
Средний балл: 4.20
----------------------------------------
🏗️ Структура кода
main.go
go
package main

import (
    "bufio"
    "fmt"
    "os"
    "strings"
)

func main() {
    fmt.Println("Добро пожаловать в журнал!")
    students := make(map[string]Student)
    reader := bufio.NewReader(os.Stdin)
    
    for {
        // Основной цикл обработки команд
    }
}
student.go
go
package main

type Student struct {
    ФИО         string
    Оценки      []int
    СреднийБалл float64
}

func (s *Student) calculateAverage() {
    // Метод для расчета среднего балла
    if len(s.Оценки) == 0 {
        s.СреднийБалл = 0.0
        return
    }
    
    sum := 0
    for _, grade := range s.Оценки {
        sum += grade
    }
    s.СреднийБалл = float64(sum) / float64(len(s.Оценки))
}
handlers.go
go
package main

import (
    "bufio"
    "fmt"
    "strconv"
    "strings"
)

// Все функции обработки команд
func addStudent(students map[string]Student, reader *bufio.Reader) { ... }
func filterStudentsByAvg(students map[string]Student, threshold float64) []Student { ... }
func printStudentInfo(student Student) { ... }
func printAllStudents(students map[string]Student) { ... }
func printHelp() { ... }
func handleFilter(students map[string]Student, reader *bufio.Reader) { ... }
🔧 Технические детали
Валидация данных
go
// Проверка уникальности ФИО
if _, exists := students[fio]; exists {
    fmt.Println("Студент с таким ФИО уже существует.")
    return
}

// Проверка корректности оценок (1-5)
gradeInt, err := strconv.Atoi(gradeStr)
if err != nil || gradeInt < 1 || gradeInt > 5 {
    fmt.Printf("Оценка '%s' некорректна и будет пропущена.\n", gradeStr)
    continue
}
Алгоритм расчета среднего балла
Суммирование всех оценок

Деление суммы на количество оценок

Округление до двух знаков после запятой

Структура хранения данных
go
// Карта для быстрого доступа по ФИО
students := make(map[string]Student)

// Структура Student содержит:
type Student struct {
    ФИО         string    // Ключ в карте
    Оценки      []int     // Срез оценок
    СреднийБалл float64   // Автоматически рассчитываемое поле
}📊 Примеры данных
Входные данные
text
ФИО: Петрова Анна
Оценки: 5 4 5 5 4 3 5

ФИО: Сидоров Иван
Оценки: 3 4 3 4 3

ФИО: Козлова Мария
Оценки: 5 5 5 5
Результаты обработки
text
=== Список всех студентов ===
Студент: Петрова Анна
Оценки: [5 4 5 5 4 3 5]
Средний балл: 4.43

Студент: Сидоров Иван
Оценки: [3 4 3 4 3]
Средний балл: 3.40

Студент: Козлова Мария
Оценки: [5 5 5 5]
Средний балл: 5.00
🧪 Тестирование
Запуск тестов
bash
# Создание тестового файла
cat > test_input.txt << EOF
add
Петрова Анна
5 4 5
3 4

list
filter
4.0
list
exit
EOF

# Запуск с тестовыми данными
go run main.go student.go handlers.go < test_input.txt
Юнит-тесты
bash
# Создание тестового файла
cat > student_test.go << EOF
package main

import "testing"

func TestCalculateAverage(t *testing.T) {
    student := Student{
        ФИО:    "Тестовый Студент",
        Оценки: []int{5, 4, 3, 5},
    }
    
    student.calculateAverage()
    
    expected := 4.25
    if student.СреднийБалл != expected {
        t.Errorf("Ожидался средний балл %.2f, получен %.2f", 
                 expected, student.СреднийБалл)
    }
}
EOF

# Запуск тестов
go test -v
📈 Возможные улучшения
Версия 2.0 (планируется)
bash
# Создание ветки для новых функций
git checkout -b v2.0-features

# Планируемые улучшения:
# 1. Сохранение данных в файл (JSON/CSV)
# 2. Загрузка данных из файла
# 3. Редактирование существующих записей
# 4. Сортировка студентов по ФИО/среднему баллу
# 5. Экспорт в различные форматы
# 6. Графический интерфейс

git add .
git commit -m "feat: план улучшений для v2.0"
Версия 3.0 (долгосрочные планы)
Веб-интерфейс на Go + HTML

REST API для интеграции с другими системами

Многопользовательский режим

Статистика и аналитика

Интеграция с системами электронного журнала

🐛 Отладка и устранение ошибок
Частые ошибки и решения
bash
# Ошибка: "студент уже существует"
# Решение: проверка уникальности ФИО встроена в программу

# Ошибка: некорректные оценки
# Решение: программа автоматически пропускает оценки не от 1 до 5

# Ошибка: деление на ноль
# Решение: проверка len(s.Оценки) == 0 перед расчетом
Логирование
go
// Добавление логирования в handlers.go
import "log"

func addStudent(students map[string]Student, reader *bufio.Reader) {
    log.Printf("Начало добавления студента")
    // ... существующий код ...
    log.Printf("Студент %s успешно добавлен", fio)
}
📝 Требования к сдаче проекта
Для ПР №3/5:
bash
# 1. Создать репозиторий на GitHub
# 2. Загрузить все файлы проекта
# 3. Создать релиз с тегом v1.0.0
# 4. Подготовить архив для сдачи

git tag -a v1.0.0 -m "Версия для сдачи ПР №3/5"
git push origin --tags
git archive --format zip --output is323_pr3_5.zip HEAD
Структура архива для сдачи:
text
is323_pr3_5.zip
├── main.go
├── student.go
├── handlers.go
├── go.mod
├── README.md
└── Примеры_работы.txt
🤝 Вклад в проект
Правила коммитов
bash
# Типы коммитов:
feat:     # Новая функциональность
fix:      # Исправление ошибок
docs:     # Изменения в документации
style:    # Форматирование кода
refactor: # Рефакторинг кода
test:     # Добавление тестов

# Пример:
git commit -m "feat: добавить сохранение в файл"
git commit -m "fix: исправить расчет среднего балла при пустых оценках"
